<p id="notice"><%= notice %></p>

<h1>Disbursements</h1>

<table>
  <thead>
    <tr>
      <th>Grant</th>
      <th>Name</th>
      <th>Landlord</th>
      <th>Number of Children</th>
      <th>Move In Amount</th>
      <th>Prevention Amount</th>
      <th></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @disbursements.each do |disbursement| %>
      <tr>
        <td><%= grant.name %></td>
        <td><%= name %></td>
        <td><%= landlord %></td>
        <td><%= number_children %></td>
        <td><%= move_in_amount_cents %></td>
        <td><%= prevention_amount_cents %></td>
        <td><%= link_to 'Show', disbursement %></td>
        <td><%= link_to 'Edit', edit_disbursement_path(disbursement) %></td>
        <td><%= link_to 'Destroy', disbursement, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Disbursement', new_disbursement_path %>
